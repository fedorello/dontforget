import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Backgrounds
  static const Color background = Color(0xFFFAFAF7);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF5F5F0);

  // Primary — soft green (health)
  static const Color primary = Color(0xFF4CAF82);
  static const Color primaryLight = Color(0xFFE8F5EE);
  static const Color primaryDark = Color(0xFF2E7D56);

  // Secondary — calm blue
  static const Color secondary = Color(0xFF5B9BD5);
  static const Color secondaryLight = Color(0xFFE8F2FB);

  // Status
  static const Color warning = Color(0xFFF5A623);
  static const Color warningLight = Color(0xFFFFF3E0);
  static const Color error = Color(0xFFE57373);
  static const Color errorLight = Color(0xFFFFEBEE);
  static const Color success = Color(0xFF4CAF82);
  static const Color successLight = Color(0xFFE8F5EE);

  // Text
  static const Color textPrimary = Color(0xFF1A1A2E);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textHint = Color(0xFFB0B7C3);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // Borders
  static const Color border = Color(0xFFE5E7EB);
  static const Color borderLight = Color(0xFFF3F4F6);

  // Shadow
  static const Color shadow = Color(0x0A000000);

  // Compatibility colors
  static const Color compatOk = Color(0xFF4CAF82);
  static const Color compatCaution = Color(0xFFF5A623);
  static const Color compatDanger = Color(0xFFE57373);
}
